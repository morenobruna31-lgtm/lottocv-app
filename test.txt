<!DOCTYPE html>
<html lang="pt">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>LottoCV — Jogos Sociais de Cabo Verde</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<style>
:root {
  --bg:#0a0c10;--bg2:#111520;--bg3:#181d2a;
  --border:rgba(255,255,255,0.07);
  --gold:#f5c842;--gold2:#e8a020;
  --cyan:#00d4aa;--red:#ff4d6d;--blue:#3a7bd5;
  --text:#e8eaf0;--muted:#6b7280;
  --hot:#ff6b35;--cold:#4a9eff;--warm:#9b8ed6;
  --radius:12px;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
body{background:var(--bg);color:var(--text);font-family:'DM Sans',sans-serif;min-height:100vh;overflow-x:hidden}
.app{max-width:1400px;margin:0 auto;padding:0 24px 60px}

/* Header */
header{display:flex;align-items:center;justify-content:space-between;padding:28px 0 32px;border-bottom:1px solid var(--border);flex-wrap:wrap;gap:12px}
.logo-name{font-family:'Bebas Neue',sans-serif;font-size:34px;letter-spacing:3px;background:linear-gradient(135deg,var(--gold),var(--gold2));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.logo-tag{font-size:11px;color:var(--muted);letter-spacing:2px;text-transform:uppercase;margin-left:10px}
.dot{width:7px;height:7px;border-radius:50%;background:var(--cyan);animation:pulse 2s infinite;display:inline-block;margin-right:6px}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.3}}
.scrape-btn{display:inline-flex;align-items:center;gap:8px;padding:9px 18px;background:transparent;border:1px solid rgba(0,212,170,0.3);border-radius:8px;color:var(--cyan);font-family:'DM Sans',sans-serif;font-size:13px;cursor:pointer;transition:all .2s}
.scrape-btn:hover{background:rgba(0,212,170,0.08);border-color:rgba(0,212,170,0.5)}
.spinner{width:16px;height:16px;border:2px solid rgba(0,212,170,0.2);border-top-color:var(--cyan);border-radius:50%;animation:spin .8s linear infinite;display:none}
@keyframes spin{to{transform:rotate(360deg)}}

/* Countdown + Budget row */
.top-row{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin:28px 0 20px}
.countdown-card{background:var(--bg2);border:1px solid rgba(0,212,170,0.2);border-radius:20px;padding:28px 32px;position:relative;overflow:hidden}
.countdown-card::before{content:'';position:absolute;top:-50px;right:-50px;width:160px;height:160px;border-radius:50%;background:radial-gradient(circle,rgba(0,212,170,0.07),transparent 70%);pointer-events:none}
.budget-card{background:var(--bg2);border:1px solid rgba(245,200,66,0.18);border-radius:20px;padding:28px 32px;position:relative;overflow:hidden}
.budget-card::before{content:'';position:absolute;top:-50px;right:-50px;width:160px;height:160px;border-radius:50%;background:radial-gradient(circle,rgba(245,200,66,0.06),transparent 70%);pointer-events:none}
.card-label{font-size:10px;letter-spacing:2.5px;text-transform:uppercase;color:var(--cyan);font-weight:600;margin-bottom:10px}
.card-label-gold{color:var(--gold)}
.countdown-digits{display:flex;gap:12px;align-items:flex-end;margin:8px 0}
.cd-block{text-align:center}
.cd-num{font-family:'Bebas Neue',sans-serif;font-size:52px;line-height:1;color:var(--text);letter-spacing:2px;min-width:60px}
.cd-unit{font-size:10px;color:var(--muted);letter-spacing:2px;text-transform:uppercase;margin-top:2px}
.cd-sep{font-family:'Bebas Neue',sans-serif;font-size:42px;color:var(--muted);margin-bottom:14px;opacity:.4}
.next-draw-info{margin-top:10px;font-size:12px;color:var(--muted)}
.next-draw-info b{color:var(--cyan)}

/* Budget */
.budget-amount{font-family:'Bebas Neue',sans-serif;font-size:52px;line-height:1;letter-spacing:2px;color:var(--gold);margin:6px 0}
.budget-sub{font-size:12px;color:var(--muted);margin-bottom:16px}
.budget-bar-wrap{margin-top:8px}
.budget-bar-label{display:flex;justify-content:space-between;font-size:11px;color:var(--muted);margin-bottom:6px}
.budget-bar{height:6px;background:rgba(255,255,255,0.07);border-radius:3px;overflow:hidden}
.budget-bar-fill{height:100%;border-radius:3px;background:linear-gradient(90deg,var(--gold),var(--gold2));transition:width .8s ease}
.budget-chips{display:flex;gap:10px;margin-top:14px}
.bchip{flex:1;background:var(--bg3);border:1px solid var(--border);border-radius:8px;padding:10px 12px;text-align:center}
.bchip-label{font-size:9px;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted)}
.bchip-val{font-family:'Bebas Neue',sans-serif;font-size:22px;color:var(--text);margin-top:2px;letter-spacing:1px}

/* Jackpot hero */
.jackpot-hero{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:20px}
.jcard{background:var(--bg2);border:1px solid rgba(245,200,66,0.18);border-radius:16px;padding:24px 28px;position:relative;overflow:hidden}
.jcard::before{content:'';position:absolute;top:-40px;right:-40px;width:130px;height:130px;border-radius:50%;background:radial-gradient(circle,rgba(245,200,66,0.06),transparent 70%);pointer-events:none}
.jcard2{border-color:rgba(0,212,170,0.15)}
.jcard2::before{background:radial-gradient(circle,rgba(0,212,170,0.05),transparent 70%)}
.jlabel{font-size:10px;letter-spacing:2.5px;text-transform:uppercase;color:var(--gold);font-weight:600;margin-bottom:8px}
.jlabel2{color:var(--cyan)}
.jval{font-family:'Bebas Neue',sans-serif;font-size:52px;line-height:1;letter-spacing:2px}
.junit{font-size:14px;color:var(--muted);margin-left:4px}
.jbar{height:3px;background:rgba(255,255,255,0.06);border-radius:2px;margin-top:12px;overflow:hidden}
.jbar-fill{height:100%;background:linear-gradient(90deg,var(--gold),var(--gold2));transition:width 1s}
.mode-badge{display:inline-flex;align-items:center;gap:6px;padding:5px 12px;border-radius:20px;font-size:11px;font-weight:600;letter-spacing:1.5px;text-transform:uppercase;margin-top:12px}
.mode-concentrado{background:rgba(245,200,66,0.12);color:var(--gold);border:1px solid rgba(245,200,66,0.25)}
.mode-semanal{background:rgba(0,212,170,0.10);color:var(--cyan);border:1px solid rgba(0,212,170,0.2)}

/* Cards */
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px}
.card{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:24px}
.card-title{font-size:10px;font-weight:600;letter-spacing:2.5px;text-transform:uppercase;color:var(--muted);margin-bottom:18px;display:flex;align-items:center;gap:6px}
.card-title::before{content:'';display:block;width:2px;height:12px;border-radius:1px;background:var(--gold)}

/* Combinações */
.combo-grid{display:flex;flex-direction:column;gap:10px}
.combo-row{display:flex;align-items:center;gap:10px;padding:12px 14px;background:var(--bg3);border:1px solid var(--border);border-radius:10px;cursor:pointer;transition:all .2s}
.combo-row:hover{border-color:rgba(245,200,66,0.3)}
.combo-idx{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted);min-width:18px}
.balls{display:flex;gap:6px;flex:1}
.ball{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:12px;font-weight:600;flex-shrink:0}
.ball.hot{background:rgba(255,107,53,0.15);color:var(--hot);border:1px solid rgba(255,107,53,0.3)}
.ball.cold{background:rgba(74,158,255,0.12);color:var(--cold);border:1px solid rgba(74,158,255,0.25)}
.ball.warm{background:rgba(155,142,214,0.12);color:var(--warm);border:1px solid rgba(155,142,214,0.2)}
.copy-btn{font-size:11px;color:var(--muted);cursor:pointer;padding:3px 7px;border-radius:5px;border:1px solid transparent;background:transparent;transition:all .2s}
.copy-btn:hover{color:var(--text);border-color:var(--border)}
.regen-btn{display:inline-flex;align-items:center;gap:5px;padding:6px 12px;background:rgba(245,200,66,0.1);border:1px solid rgba(245,200,66,0.25);border-radius:7px;color:var(--gold);font-size:11px;cursor:pointer;float:right;transition:all .2s}
.regen-btn:hover{background:rgba(245,200,66,0.18)}

/* Heatmap */
.heatmap{display:grid;grid-template-columns:repeat(9,1fr);gap:5px}
.hm-cell{aspect-ratio:1;border-radius:7px;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:11px;font-weight:600;cursor:default;transition:transform .15s;position:relative}
.hm-cell:hover{transform:scale(1.15);z-index:2}
.hm-cell .tip{display:none;position:absolute;bottom:calc(100% + 5px);left:50%;transform:translateX(-50%);background:#1e2535;border:1px solid var(--border);border-radius:5px;padding:3px 7px;font-size:10px;white-space:nowrap;color:var(--text);z-index:10}
.hm-cell:hover .tip{display:block}

/* Charts */
.freq-chart-wrap{position:relative;height:220px}
.evo-chart-wrap{position:relative;height:190px}

/* Histórico */
.hist-table{width:100%;border-collapse:collapse;font-size:13px}
.hist-table th{text-align:left;padding:7px 10px;font-size:9px;letter-spacing:2px;text-transform:uppercase;color:var(--muted);font-weight:600;border-bottom:1px solid var(--border)}
.hist-table td{padding:9px 10px;border-bottom:1px solid rgba(255,255,255,0.03);font-family:'JetBrains Mono',monospace;font-size:11px}
.hist-table tr:hover td{background:rgba(255,255,255,0.02)}
.mini-balls{display:flex;gap:4px}
.mini-ball{width:24px;height:24px;border-radius:50%;background:var(--bg3);border:1px solid var(--border);display:flex;align-items:center;justify-content:center;font-size:9px}

/* ── LottoVision AI Chat ── */
.lv-section{margin-top:20px}
.lv-card{background:linear-gradient(135deg,rgba(58,123,213,0.08) 0%,var(--bg2) 60%);border:1px solid rgba(58,123,213,0.25);border-radius:20px;overflow:hidden}
.lv-header{display:flex;align-items:center;gap:14px;padding:20px 28px;border-bottom:1px solid rgba(58,123,213,0.15)}
.lv-avatar{width:44px;height:44px;border-radius:12px;background:linear-gradient(135deg,#3a7bd5,#00d4aa);display:flex;align-items:center;justify-content:center;font-size:22px;flex-shrink:0}
.lv-title{font-family:'Bebas Neue',sans-serif;font-size:24px;letter-spacing:2px;background:linear-gradient(135deg,#3a7bd5,#00d4aa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.lv-sub{font-size:12px;color:var(--muted);margin-top:1px}
.lv-online{display:flex;align-items:center;gap:6px;font-size:11px;color:var(--cyan);margin-left:auto}

.lv-messages{height:320px;overflow-y:auto;padding:20px 24px;display:flex;flex-direction:column;gap:12px;scroll-behavior:smooth}
.lv-messages::-webkit-scrollbar{width:4px}
.lv-messages::-webkit-scrollbar-track{background:transparent}
.lv-messages::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.1);border-radius:2px}

.lv-msg{max-width:80%;padding:12px 16px;border-radius:14px;font-size:14px;line-height:1.6;animation:msgIn .3s ease}
@keyframes msgIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}
.lv-msg-bot{background:var(--bg3);border:1px solid var(--border);border-radius:14px 14px 14px 4px;align-self:flex-start;color:var(--text)}
.lv-msg-user{background:linear-gradient(135deg,rgba(58,123,213,0.3),rgba(0,212,170,0.2));border:1px solid rgba(58,123,213,0.2);border-radius:14px 14px 4px 14px;align-self:flex-end;color:var(--text)}
.lv-msg-bot b{color:var(--gold)}
.lv-msg-bot .combo-highlight{font-family:'JetBrains Mono',monospace;background:rgba(245,200,66,0.1);border:1px solid rgba(245,200,66,0.2);border-radius:6px;padding:2px 8px;font-size:13px;color:var(--gold);display:inline-block;margin:2px 0}
.typing{display:flex;gap:5px;align-items:center;padding:14px 18px}
.typing span{width:7px;height:7px;border-radius:50%;background:var(--muted);animation:bounce .8s infinite}
.typing span:nth-child(2){animation-delay:.15s}
.typing span:nth-child(3){animation-delay:.3s}
@keyframes bounce{0%,60%,100%{transform:translateY(0)}30%{transform:translateY(-6px)}}

.lv-suggestions{display:flex;gap:8px;padding:0 24px 14px;flex-wrap:wrap}
.lv-sug{padding:7px 14px;background:var(--bg3);border:1px solid rgba(58,123,213,0.2);border-radius:20px;font-size:12px;color:var(--muted);cursor:pointer;transition:all .2s;white-space:nowrap}
.lv-sug:hover{color:var(--cyan);border-color:rgba(0,212,170,0.4);background:rgba(0,212,170,0.06)}

.lv-input-row{display:flex;gap:10px;padding:16px 24px;border-top:1px solid rgba(58,123,213,0.12);background:rgba(0,0,0,0.2)}
.lv-input{flex:1;background:var(--bg3);border:1px solid rgba(58,123,213,0.2);border-radius:12px;padding:12px 16px;color:var(--text);font-family:'DM Sans',sans-serif;font-size:14px;outline:none;transition:border-color .2s}
.lv-input:focus{border-color:rgba(58,123,213,0.5)}
.lv-input::placeholder{color:var(--muted)}
.lv-send{width:44px;height:44px;border-radius:12px;background:linear-gradient(135deg,#3a7bd5,#00d4aa);border:none;color:#fff;font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:opacity .2s;flex-shrink:0}
.lv-send:hover{opacity:.85}
.lv-send:disabled{opacity:.4;cursor:not-allowed}

/* Recomendação */
.rec-card{background:linear-gradient(135deg,rgba(245,200,66,0.05),var(--bg2));border:1px solid rgba(245,200,66,0.18);border-radius:var(--radius);padding:22px;margin-bottom:16px}
.rec-text{font-size:13px;color:var(--muted);line-height:1.7;margin-top:8px}
.budget-row{display:flex;gap:12px;margin-top:14px}
.bc{flex:1;background:var(--bg3);border:1px solid var(--border);border-radius:8px;padding:12px 14px}
.bc-l{font-size:9px;letter-spacing:2px;text-transform:uppercase;color:var(--muted)}
.bc-v{font-family:'Bebas Neue',sans-serif;font-size:24px;color:var(--gold);letter-spacing:1px;margin-top:3px}

/* Freq legend */
.freq-legend{display:flex;gap:16px;margin-top:12px;font-size:11px;color:var(--muted)}
.freq-legend span{display:flex;align-items:center;gap:5px}
.leg-dot{width:9px;height:9px;border-radius:2px;flex-shrink:0}

/* Responsive */
@media(max-width:900px){
  .top-row,.jackpot-hero,.grid2{grid-template-columns:1fr}
  .jval,.cd-num,.budget-amount{font-size:40px}
  .heatmap{grid-template-columns:repeat(7,1fr)}
}
</style>
</head>
<body>
<div class="app">

<!-- HEADER -->
<header>
  <div>
    <span class="logo-name">LOTTOCV</span>
    <span class="logo-tag">Jogos Sociais · Cabo Verde</span>
  </div>
  <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap">
    <div style="font-size:12px;color:var(--muted)">
      <span class="dot"></span><span id="last-update">A carregar...</span>
    </div>
    <button class="scrape-btn" onclick="triggerScrape()">
      <div class="spinner" id="scrape-spinner"></div>
      ↻ Atualizar dados
    </button>
  </div>
</header>

<!-- COUNTDOWN + BUDGET -->
<div class="top-row">
  <div class="countdown-card">
    <div class="card-label">⏱ Próximo Sorteio</div>
    <div class="countdown-digits">
      <div class="cd-block"><div class="cd-num" id="cd-d">--</div><div class="cd-unit">dias</div></div>
      <div class="cd-sep">:</div>
      <div class="cd-block"><div class="cd-num" id="cd-h">--</div><div class="cd-unit">horas</div></div>
      <div class="cd-sep">:</div>
      <div class="cd-block"><div class="cd-num" id="cd-m">--</div><div class="cd-unit">min</div></div>
      <div class="cd-sep">:</div>
      <div class="cd-block"><div class="cd-num" id="cd-s">--</div><div class="cd-unit">seg</div></div>
    </div>
    <div class="next-draw-info">Próximo sorteio: <b id="next-draw-date">--</b> às 19h15</div>
    <div style="margin-top:6px;font-size:11px;color:var(--muted)">Sorteio: todos os Sábados às 19h15</div>
  </div>

    <div class="budget-card">
    <div class="card-label card-label-gold">💳 O Meu Orçamento Mensal</div>
    <div style="display:flex;align-items:flex-end;gap:8px;margin:4px 0 2px">
      <div class="budget-amount" id="budget-restante">1.000</div>
      <div style="font-size:14px;color:var(--muted);margin-bottom:12px">ECV restantes</div>
    </div>
    <div class="budget-sub" id="budget-mode-text">A carregar...</div>
    <div class="budget-bar-wrap" style="margin:10px 0">
      <div class="budget-bar-label">
        <span id="budget-used-label">0 ECV gastos</span>
        <span id="budget-total-label">1.000 ECV total</span>
      </div>
      <div class="budget-bar"><div class="budget-bar-fill" id="budget-bar-fill" style="width:0%"></div></div>
    </div>
    <div style="display:flex;gap:8px;margin-top:14px;align-items:center;flex-wrap:wrap">
      <button onclick="registarGasto(100)" style="flex:1;padding:10px;background:linear-gradient(135deg,var(--gold),var(--gold2));border:none;border-radius:8px;color:#111;font-weight:700;font-size:14px;cursor:pointer;min-width:80px">− 100 ECV</button>
      <div style="display:flex;gap:6px;flex:2">
        <input id="gasto-custom" type="number" min="1" max="1000" placeholder="outro valor" style="flex:1;background:var(--bg3);border:1px solid var(--border);border-radius:8px;padding:9px 10px;color:var(--text);font-size:13px;outline:none;min-width:0">
        <button onclick="registarGastoCustom()" style="padding:9px 14px;background:rgba(245,200,66,0.15);border:1px solid rgba(245,200,66,0.3);border-radius:8px;color:var(--gold);font-size:13px;cursor:pointer">Gastar</button>
      </div>
      <button onclick="desfazerGasto()" title="Desfazer último" style="padding:9px 12px;background:rgba(255,77,109,0.1);border:1px solid rgba(255,77,109,0.2);border-radius:8px;color:#ff4d6d;font-size:13px;cursor:pointer">↩ Desfazer</button>
    </div>
    <div id="gastos-lista" style="margin-top:12px;max-height:110px;overflow-y:auto;display:flex;flex-direction:column;gap:4px"></div>
  </div>
</div>

<!-- JACKPOT HERO -->
<div class="jackpot-hero">
  <div class="jcard">
    <div class="jlabel">🎰 Totoloto — Jackpot Acumulado</div>
    <div><span class="jval" id="jp-totoloto">—</span><span class="junit">contos</span></div>
    <div style="font-size:12px;color:var(--muted);margin-top:6px">
      📅 <span id="jp-data">—</span> &nbsp;·&nbsp; 🎫 Concurso <span id="jp-concurso">—</span>
    </div>
    <div class="jbar"><div class="jbar-fill" id="jp-bar" style="width:0%"></div></div>
    <div style="font-size:10px;color:var(--muted);text-align:right;margin-top:4px" id="jp-pct">0% do máximo</div>
    <div id="mode-badge-hero"></div>
  </div>
  <div class="jcard jcard2">
    <div class="jlabel jlabel2">🃏 Joker — Jackpot</div>
    <div><span class="jval" id="jp-joker" style="font-size:40px">—</span><span class="junit">contos</span></div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:16px">
      <div class="bchip"><div class="bchip-label">Sorteios</div><div class="bchip-val" id="total-sorteios">—</div></div>
      <div class="bchip"><div class="bchip-label">Top quente</div><div class="bchip-val" style="font-size:14px;margin-top:5px;color:var(--hot)" id="top-hot">—</div></div>
    </div>
  </div>
</div>

<!-- RECOMENDAÇÃO -->
<div class="rec-card">
  <div style="display:flex;align-items:center;justify-content:space-between">
    <div class="card-title" style="margin:0">Recomendação de Aposta</div>
    <span id="rec-mode-badge"></span>
  </div>
  <div class="rec-text" id="rec-texto">A calcular recomendação...</div>
  <div class="budget-row">
    <div class="bc"><div class="bc-l">Orçamento a usar</div><div class="bc-v" id="rec-orcamento">—</div></div>
    <div class="bc"><div class="bc-l">Modo</div><div class="bc-v" id="rec-modo" style="font-size:16px;margin-top:6px">—</div></div>
    <div class="bc"><div class="bc-l">Sorteios históricos</div><div class="bc-v" id="rec-historico">—</div></div>
  </div>
</div>

<!-- COMBINAÇÕES + HEATMAP -->
<div class="grid2">
  <div class="card">
    <div class="card-title">
      Combinações Sugeridas
      <button class="regen-btn" onclick="gerarCombinacoes()">⟳ Gerar novas</button>
    </div>
    <div class="combo-grid" id="combos-container">
      <div style="color:var(--muted);font-size:13px">A gerar...</div>
    </div>
    <div class="freq-legend" style="margin-top:14px">
      <span><span class="leg-dot" style="background:var(--hot)"></span>Quente</span>
      <span><span class="leg-dot" style="background:var(--cold)"></span>Frio</span>
      <span><span class="leg-dot" style="background:var(--warm)"></span>Morno</span>
    </div>
  </div>
  <div class="card">
    <div class="card-title">Mapa de Frequência (1–45)</div>
    <div class="heatmap" id="heatmap-container"></div>
    <div class="freq-legend" style="margin-top:12px">
      <span><span class="leg-dot" style="background:var(--hot)"></span>Mais sorteado</span>
      <span><span class="leg-dot" style="background:var(--cold)"></span>Menos sorteado</span>
    </div>
  </div>
</div>

<!-- GRÁFICO FREQUÊNCIAS -->
<div class="card" style="margin-bottom:16px">
  <div class="card-title">Frequência por Número</div>
  <div class="freq-chart-wrap"><canvas id="freq-chart"></canvas></div>
</div>

<!-- EVOLUÇÃO + HISTÓRICO -->
<div class="grid2">
  <div class="card">
    <div class="card-title">Evolução do Jackpot</div>
    <div class="evo-chart-wrap"><canvas id="evo-chart"></canvas></div>
  </div>
  <div class="card">
    <div class="card-title">Últimos Sorteios</div>
    <table class="hist-table">
      <thead><tr><th>Concurso</th><th>Números</th><th>Data</th></tr></thead>
      <tbody id="historico-tbody"><tr><td colspan="3" style="color:var(--muted)">A carregar...</td></tr></tbody>
    </table>
  </div>
</div>


<!-- APOSTAS JOGADAS -->
<div style="margin-bottom:20px">
  <div class="card">
    <div class="card-title">
      🎫 As Minhas Apostas
      <button class="regen-btn" style="float:right" onclick="toggleApostaForm()">+ Registar aposta</button>
    </div>
    <div id="aposta-form" style="display:none;margin-bottom:20px;padding:18px;background:var(--bg3);border-radius:10px;border:1px solid rgba(245,200,66,0.2)">
      <div style="font-size:12px;color:var(--muted);margin-bottom:12px">Introduz os 6 números que jogaste:</div>
      <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:14px" id="num-inputs">
        <input class="num-inp" type="number" min="1" max="45" placeholder="N1">
        <input class="num-inp" type="number" min="1" max="45" placeholder="N2">
        <input class="num-inp" type="number" min="1" max="45" placeholder="N3">
        <input class="num-inp" type="number" min="1" max="45" placeholder="N4">
        <input class="num-inp" type="number" min="1" max="45" placeholder="N5">
        <input class="num-inp" type="number" min="1" max="45" placeholder="N6">
      </div>
      <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
        <div style="font-size:12px;color:var(--muted)">Concurso:</div>
        <input id="concurso-inp" type="text" placeholder="ex: 13/2026" style="background:var(--bg2);border:1px solid var(--border);border-radius:7px;padding:7px 12px;color:var(--text);font-size:13px;width:120px;outline:none">
        <div style="font-size:12px;color:var(--muted)">Custo: <b style="color:var(--gold)">100 ECV</b> (30 Totoloto + 70 Joker)</div>
        <button onclick="registarAposta()" style="margin-left:auto;padding:9px 20px;background:linear-gradient(135deg,var(--gold),var(--gold2));border:none;border-radius:8px;color:#111;font-weight:600;font-size:13px;cursor:pointer">Guardar aposta</button>
        <div id="aposta-msg" style="font-size:12px;color:var(--cyan)"></div>
      </div>
    </div>
    <div id="apostas-lista"><div style="color:var(--muted);font-size:13px;padding:8px 0">Sem apostas registadas ainda.</div></div>
    <div style="margin-top:16px;display:flex;gap:10px;align-items:center;flex-wrap:wrap">
      <button onclick="verificarResultados()" style="padding:9px 20px;background:rgba(0,212,170,0.12);border:1px solid rgba(0,212,170,0.3);border-radius:8px;color:var(--cyan);font-size:13px;font-weight:500;cursor:pointer">
        🔍 Verificar resultado do último sorteio
      </button>
      <div id="verificar-msg" style="font-size:13px;color:var(--muted)"></div>
    </div>
    <div id="resultado-verificacao" style="display:none;margin-top:18px"></div>
  </div>
</div>


<!-- MODO MENSAL -->
<div style="margin-bottom:20px">
  <div class="card">
    <div class="card-title">📅 Modo do Mês</div>
    <div style="display:flex;gap:10px;margin-bottom:18px;flex-wrap:wrap">
      <button id="btn-toto" onclick="setModo('MES_TOTOLOTO')" style="flex:1;padding:12px;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer;transition:all .2s;border:2px solid var(--gold);background:rgba(245,200,66,0.15);color:var(--gold)">
        🎯 Mês Totoloto<br><span style="font-size:11px;font-weight:400;opacity:.8">8 chaves × 30 ECV × 4 sorteios = 960 ECV</span>
      </button>
      <button id="btn-comp" onclick="setModo('MES_COMPLETO')" style="flex:1;padding:12px;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer;transition:all .2s;border:2px solid var(--border);background:transparent;color:var(--muted)">
        🎰 Mês Completo<br><span style="font-size:11px;font-weight:400;opacity:.8">Totoloto + Joker (100 ECV/chave)</span>
      </button>
    </div>

    <div id="modo-info" style="padding:16px;background:var(--bg3);border-radius:10px;border-left:3px solid var(--gold);margin-bottom:18px">
      <div style="font-size:13px;color:var(--text);line-height:1.7" id="modo-justificacao">A carregar...</div>
    </div>

    <!-- 8 chaves do mês -->
    <div class="card-title" style="margin-bottom:12px">🎲 As Tuas 8 Chaves para este Sorteio</div>
    <div id="chaves-mes" style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
      <div style="color:var(--muted);font-size:13px">A gerar...</div>
    </div>
    <button onclick="gerarChavesMes()" style="margin-top:14px;padding:9px 18px;background:rgba(245,200,66,0.1);border:1px solid rgba(245,200,66,0.3);border-radius:8px;color:var(--gold);font-size:12px;cursor:pointer">⟳ Gerar novas chaves</button>

    <!-- Análise próximo mês -->
    <div style="margin-top:20px;padding:14px;background:rgba(0,212,170,0.05);border:1px solid rgba(0,212,170,0.15);border-radius:10px">
      <div style="font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--cyan);margin-bottom:8px">📊 Análise — Próximo Mês</div>
      <div id="proximo-mes-text" style="font-size:13px;color:var(--muted);line-height:1.6">A analisar...</div>
    </div>
  </div>
</div>

<!-- BÊNÇÃO E VERSÍCULOS -->
<div style="margin-bottom:20px">
  <div class="card" style="background:linear-gradient(135deg,rgba(139,90,43,0.08),var(--bg2));border-color:rgba(210,180,140,0.2)">
    <div class="card-title" style="color:#d4a96a">✝️ Bênção & Palavra de Deus</div>
    <div id="oracao-div" style="font-size:14px;color:#c4956a;font-style:italic;line-height:1.7;margin-bottom:16px;padding:14px;background:rgba(210,180,140,0.06);border-radius:8px;border-left:3px solid rgba(210,180,140,0.3)">
      A carregar...
    </div>
    <div id="versiculos-div" style="display:flex;flex-direction:column;gap:12px"></div>
    <div style="margin-top:14px;font-size:12px;color:rgba(210,180,140,0.6);text-align:center" id="bencao-msg"></div>
    <button onclick="loadBencao()" style="display:block;margin:14px auto 0;padding:8px 20px;background:rgba(210,180,140,0.1);border:1px solid rgba(210,180,140,0.25);border-radius:8px;color:#d4a96a;font-size:12px;cursor:pointer">🙏 Nova bênção</button>
  </div>
</div>

<!-- LOTTOVISION AI -->
<div class="lv-section">
  <div class="lv-card">
    <div class="lv-header">
      <div class="lv-avatar">🔮</div>
      <div>
        <div class="lv-title">LottoVision</div>
        <div class="lv-sub">Assistente IA · Análise & Combinações</div>
      </div>
      <div class="lv-online"><span class="dot"></span>Online</div>
    </div>

    <div class="lv-messages" id="lv-messages">
      <div class="lv-msg lv-msg-bot">
        👋 Olá! Sou o <b>LottoVision</b>, o teu assistente inteligente para os Jogos Sociais de Cabo Verde.<br><br>
        Posso gerar combinações, analisar frequências e recomendar estratégias de aposta. Em que posso ajudar?
      </div>
    </div>

    <div class="lv-suggestions">
      <div class="lv-sug" onclick="sendSuggestion(this)">🎲 Gera 5 combinações para hoje</div>
      <div class="lv-sug" onclick="sendSuggestion(this)">🔥 Quais os números mais quentes?</div>
      <div class="lv-sug" onclick="sendSuggestion(this)">💰 Devo apostar esta semana?</div>
      <div class="lv-sug" onclick="sendSuggestion(this)">📊 Analisa o jackpot atual</div>
      <div class="lv-sug" onclick="sendSuggestion(this)">🃏 Combinação para o Joker</div>
    </div>

    <div class="lv-input-row">
      <input class="lv-input" id="lv-input" placeholder="Pergunta ao LottoVision..." onkeydown="if(event.key==='Enter')sendToLV()">
      <button class="lv-send" id="lv-send-btn" onclick="sendToLV()">➤</button>
    </div>
  </div>
</div>

<div style="text-align:center;margin-top:32px;font-size:11px;color:var(--muted)">
  ⚠️ A análise de frequência é estatística descritiva. Joga com responsabilidade e dentro das tuas possibilidades.
</div>

</div><!-- .app -->

<script>
const API = '';
let freqData=[], hotNums=new Set(), coldNums=new Set();
let freqChart, evoChart, freqAnimFrame;
const fmt = function(n){ return Number(n).toLocaleString('pt-PT'); };

async function api(path, opts){
  try{
    const r = await fetch(API+path, opts);
    if(!r.ok) throw new Error(r.status);
    return await r.json();
  }catch(e){ console.warn('API erro:',path,e); return null; }
}

/* ── Countdown Sábado 19h15 ── */
function calcNextDraw(){
  const now = new Date();
  const cvNow = new Date(now.getTime() - 1*60*60*1000);
  const day = cvNow.getUTCDay();
  const h = cvNow.getUTCHours(), m = cvNow.getUTCMinutes();
  let daysUntil = (6 - day + 7) % 7;
  if(daysUntil === 0 && (h > 20 || (h === 20 && m >= 15))) daysUntil = 7;
  if(daysUntil === 0) daysUntil = 7;
  const next = new Date(cvNow);
  next.setUTCDate(next.getUTCDate() + daysUntil);
  next.setUTCHours(20, 15, 0, 0);
  return new Date(next.getTime() + 1*60*60*1000);
}

function updateCountdown(){
  const next = calcNextDraw();
  const diff = next - new Date();
  if(diff <= 0){ updateCountdown(); return; }
  const d = Math.floor(diff/86400000);
  const h = Math.floor((diff%86400000)/3600000);
  const m = Math.floor((diff%3600000)/60000);
  const s = Math.floor((diff%60000)/1000);
  document.getElementById('cd-d').textContent = String(d).padStart(2,'0');
  document.getElementById('cd-h').textContent = String(h).padStart(2,'0');
  document.getElementById('cd-m').textContent = String(m).padStart(2,'0');
  document.getElementById('cd-s').textContent = String(s).padStart(2,'0');
  const opts = {weekday:'long',day:'numeric',month:'long'};
  document.getElementById('next-draw-date').textContent = next.toLocaleDateString('pt-PT', opts);
}
setInterval(updateCountdown, 1000);
updateCountdown();

/* ── Orçamento Tracker ── */
function mesAtual(){
  const n = new Date();
  return n.getFullYear()+'-'+String(n.getMonth()+1).padStart(2,'0');
}

async function loadBudget(){
  const d = await api('/api/orcamento/'+mesAtual());
  if(!d) return;
  const restante = d.restante, gasto = d.gasto, total = d.total;
  const pct = Math.min(100, (gasto/total)*100);
  document.getElementById('budget-restante').textContent = Math.round(restante).toLocaleString('pt-PT');
  document.getElementById('budget-used-label').textContent = gasto+' ECV gastos';
  document.getElementById('budget-total-label').textContent = total+' ECV total';
  document.getElementById('budget-bar-fill').style.width = pct+'%';
  const barEl = document.getElementById('budget-bar-fill');
  if(pct >= 90) barEl.style.background = 'linear-gradient(90deg,#ff4d6d,#c0392b)';
  else if(pct >= 60) barEl.style.background = 'linear-gradient(90deg,#f5c842,#e8a020)';
  else barEl.style.background = 'linear-gradient(90deg,#00d4aa,#0f6e56)';
  const sorteiosFeitos = d.gastos_lista ? d.gastos_lista.length : 0;
  document.getElementById('budget-mode-text').textContent = sorteiosFeitos+'/4 sorteios jogados este mês';
  const lista = document.getElementById('gastos-lista');
  if(d.gastos_lista && d.gastos_lista.length > 0){
    let html = '';
    d.gastos_lista.slice(0,5).forEach(function(g){
      html += '<div style="display:flex;justify-content:space-between;align-items:center;padding:5px 8px;background:var(--bg3);border-radius:6px;font-size:12px">';
      html += '<span style="color:var(--muted)">'+(g.descricao||'Aposta')+'</span>';
      html += '<span style="color:#ff4d6d;font-weight:600;font-family:monospace">−'+g.valor+' ECV</span></div>';
    });
    lista.innerHTML = html;
  } else {
    lista.innerHTML = '<div style="font-size:11px;color:var(--muted);padding:4px 0">Sem gastos registados este mês</div>';
  }
}

async function registarGasto(valor){
  const hoje = new Date().toLocaleDateString('pt-PT',{weekday:'short',day:'numeric',month:'short'});
  const desc = 'Sorteio — '+hoje;
  await api('/api/orcamento/gasto', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({mes: mesAtual(), valor: valor, descricao: desc})
  });
  await loadBudget();
}

async function registarGastoCustom(){
  const inp = document.getElementById('gasto-custom');
  const val = parseFloat(inp.value);
  if(!val || val <= 0){ inp.style.borderColor='rgba(255,77,109,0.5)'; return; }
  inp.style.borderColor='';
  inp.value = '';
  await registarGasto(val);
}

async function desfazerGasto(){
  await api('/api/orcamento/desfazer/'+mesAtual(), {method:'DELETE'});
  await loadBudget();
}

/* ── Jackpot ── */
async function loadJackpot(){
  const d = await api('/api/jackpot');
  if(!d) return;
  const tv = d.totoloto.valor_contos;
  document.getElementById('jp-totoloto').textContent = fmt(Math.round(tv));
  document.getElementById('jp-joker').textContent    = fmt(Math.round(d.joker.valor_contos));
  document.getElementById('jp-data').textContent     = d.totoloto.data_sorteio||'—';
  document.getElementById('jp-concurso').textContent = d.totoloto.concurso||'—';
  const pct = Math.min(100,(d.totoloto.valor/50000000)*100);
  document.getElementById('jp-bar').style.width  = pct+'%';
  document.getElementById('jp-pct').textContent  = pct.toFixed(1)+'% do máximo (50.000 contos)';
  document.getElementById('last-update').textContent = 'Atualizado: '+(d.totoloto.atualizado_em||'agora');
}

/* ── Recomendação ── */
async function loadRecomendacao(){
  const d = await api('/api/recomendacao');
  if(!d) return;
  document.getElementById('rec-texto').textContent   = d.justificacao;
  document.getElementById('rec-orcamento').textContent = fmt(d.orcamento_usar)+' ECV';
  document.getElementById('rec-modo').textContent    = d.modo;
  document.getElementById('rec-historico').textContent = d.total_sorteios_analisados;
  const isC = d.modo && d.modo.indexOf('JACKPOT') >= 0;
  const bHtml = '<span class="mode-badge '+(isC?'mode-concentrado':'mode-semanal')+'">'+(isC?'🔥 JACKPOT ALTO':'📅 SEMANAL')+'</span>';
  document.getElementById('rec-mode-badge').innerHTML  = bHtml;
  document.getElementById('mode-badge-hero').innerHTML = bHtml;
}

/* ── Modo Mensal ── */
let modoAtual = 'MES_TOTOLOTO';

function setModo(modo){
  modoAtual = modo;
  const btnT = document.getElementById('btn-toto');
  const btnC = document.getElementById('btn-comp');
  if(modo === 'MES_TOTOLOTO'){
    btnT.style.cssText = 'flex:1;padding:12px;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer;transition:all .2s;border:2px solid var(--gold);background:rgba(245,200,66,0.15);color:var(--gold)';
    btnC.style.cssText = 'flex:1;padding:12px;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer;transition:all .2s;border:2px solid var(--border);background:transparent;color:var(--muted)';
  } else {
    btnC.style.cssText = 'flex:1;padding:12px;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer;transition:all .2s;border:2px solid var(--cyan);background:rgba(0,212,170,0.12);color:var(--cyan)';
    btnT.style.cssText = 'flex:1;padding:12px;border-radius:10px;font-size:13px;font-weight:600;cursor:pointer;transition:all .2s;border:2px solid var(--border);background:transparent;color:var(--muted)';
  }
  loadModoMes();
}

async function loadModoMes(){
  const d = await api('/api/recomendacao/'+modoAtual);
  if(!d) return;
  document.getElementById('modo-justificacao').innerHTML = d.justificacao.replace(/\n/g,'<br>');
  if(freqData.length === 0){ await loadFrequencias(); }
  await gerarChavesMes();
}
async function gerarChavesMes(){
  if(freqData.length === 0){ await loadFrequencias(); }
  const n = modoAtual === 'MES_TOTOLOTO' ? 8 : 2;
  const d = await api('/api/combinacoes/'+n);
  if(!d || !d.combinacoes) return;
  const el = document.getElementById('chaves-mes');
  let html = '';
  d.combinacoes.forEach(function(combo, i){
    let balls = '';
    combo.forEach(function(num){
      const tipo = hotNums.has(num) ? 'hot' : coldNums.has(num) ? 'cold' : 'warm';
      const n2 = String(num).padStart(2,'0');
      balls += '<div class="ball '+tipo+'" style="width:28px;height:28px;font-size:11px">'+n2+'</div>';
    });
    html += '<div class="combo-row" style="padding:10px 12px" onclick="copiarCombo(['+combo.join(',')+'],this)">';
    html += '<span class="combo-idx">'+(i+1)+'</span>';
    html += '<div class="balls">'+balls+'</div>';
    html += '<button class="copy-btn">⎘</button></div>';
  });
  el.innerHTML = html;
  await loadBencao();
}

async function loadProximoMes(){
  const d = await api('/api/proximo-mes');
  if(!d) return;
  const icon = d.recomendacao === 'MES_TOTOLOTO' ? '🎯' : '🎰';
  const modo = d.recomendacao === 'MES_TOTOLOTO' ? 'Mês Totoloto' : 'Mês Completo';
  document.getElementById('proximo-mes-text').innerHTML =
    icon+' <b style="color:var(--cyan)">'+modo+'</b> — '+d.razao;
}

/* ── Bênção & Bíblia ── */
async function loadBencao(){
  const d = await api('/api/bencao');
  if(!d) return;
  document.getElementById('oracao-div').textContent = d.oracao;
  document.getElementById('bencao-msg').textContent = d.mensagem;
  const el = document.getElementById('versiculos-div');
  let html = '';
  if(d.versiculos && d.versiculos.length){
    d.versiculos.forEach(function(v){
      html += '<div style="padding:12px 16px;background:rgba(210,180,140,0.05);border:1px solid rgba(210,180,140,0.12);border-radius:8px;margin-bottom:8px">';
      html += '<div style="font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:rgba(210,180,140,0.6);margin-bottom:5px">'+v.ref+'</div>';
      html += '<div style="font-size:13px;color:#c4956a;line-height:1.7;font-style:italic">"'+v.texto+'"</div>';
      html += '</div>';
    });
  }
  el.innerHTML = html;
}

/* ── Frequências ── */
async function loadFrequencias(){
  const d = await api('/api/frequencias');
  if(!d) return;
  freqData = d.numeros;
  hotNums  = new Set(d.top_quentes);
  coldNums = new Set(d.top_frios);
  document.getElementById('total-sorteios').textContent = d.total_sorteios;
  document.getElementById('top-hot').textContent = d.top_quentes.slice(0,3).join(' · ');
  renderHeatmap(freqData);
  renderFreqChart(freqData);
}

function renderHeatmap(data){
  const maxC = Math.max.apply(null, data.map(function(d){return d.count;}));
  const el = document.getElementById('heatmap-container');
  let html = '';
  data.forEach(function(d){
    const r = maxC>0?d.count/maxC:0;
    let bg, color;
    if(d.tipo==='quente'){bg='rgba(255,107,53,'+(0.1+r*0.5)+')';color='#ff6b35';}
    else if(d.tipo==='frio'){bg='rgba(74,158,255,'+(0.08+r*0.3)+')';color='#4a9eff';}
    else{bg='rgba(155,142,214,'+(0.08+r*0.3)+')';color='#9b8ed6';}
    html += '<div class="hm-cell" style="background:'+bg+';color:'+color+'">'+String(d.numero).padStart(2,'0')+'<div class="tip">'+d.count+'×</div></div>';
  });
  el.innerHTML = html;
}

function renderFreqChart(data){
  if(freqChart) freqChart.destroy();
  if(freqAnimFrame) cancelAnimationFrame(freqAnimFrame);
  const ctx = document.getElementById('freq-chart').getContext('2d');
  const colors = data.map(function(d){
    return d.tipo==='quente'?'rgba(255,107,53,0.7)':d.tipo==='frio'?'rgba(74,158,255,0.6)':'rgba(155,142,214,0.5)';
  });
  const finalData = data.map(function(d){return d.count;});
  const animData  = data.map(function(){return 0;});
  freqChart = new Chart(ctx,{
    type:'bar',
    data:{labels:data.map(function(d){return d.numero;}),datasets:[{data:animData,backgroundColor:colors,borderRadius:3,borderSkipped:false}]},
    options:{responsive:true,maintainAspectRatio:false,animation:false,
      plugins:{legend:{display:false},tooltip:{callbacks:{label:function(c){return 'Sorteado '+finalData[c.dataIndex]+'×';}}}},
      scales:{x:{grid:{color:'rgba(255,255,255,0.04)'},ticks:{color:'#6b7280',font:{family:'JetBrains Mono',size:10}}},
              y:{grid:{color:'rgba(255,255,255,0.04)'},ticks:{color:'#6b7280',font:{size:11}},beginAtZero:true}}}
  });
  let step=0, steps=40;
  function animate(){
    step++;
    const t=step/steps, ease=1-Math.pow(1-t,3);
    freqChart.data.datasets[0].data=finalData.map(function(v){return Math.round(v*ease);});
    freqChart.update('none');
    if(step<steps) freqAnimFrame=requestAnimationFrame(animate);
  }
  requestAnimationFrame(animate);
}
setInterval(function(){ if(freqData.length) renderFreqChart(freqData); }, 30000);

async function loadEvolucao(){
  const d = await api('/api/jackpot/evolucao');
  if(!d||!d.evolucao.length) return;
  if(evoChart) evoChart.destroy();
  const ctx = document.getElementById('evo-chart').getContext('2d');
  const grad = ctx.createLinearGradient(0,0,0,190);
  grad.addColorStop(0,'rgba(245,200,66,0.25)');
  grad.addColorStop(1,'rgba(245,200,66,0)');
  evoChart = new Chart(ctx,{
    type:'line',
    data:{labels:d.evolucao.map(function(e){return e.concurso;}),datasets:[{data:d.evolucao.map(function(e){return e.jackpot/1000000;}),borderColor:'#f5c842',borderWidth:2,fill:true,backgroundColor:grad,tension:.4,pointRadius:0,pointHoverRadius:4,pointHoverBackgroundColor:'#f5c842'}]},
    options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{callbacks:{label:function(c){return c.raw.toFixed(1)+'M ECV';}}}},
      scales:{x:{display:false},y:{grid:{color:'rgba(255,255,255,0.04)'},ticks:{color:'#6b7280',font:{size:11},callback:function(v){return v+'M';}}}}}
  });
}

/* ── Combinações dashboard ── */
async function gerarCombinacoes(){
  if(freqData.length === 0){ await loadFrequencias(); }
  const d = await api('/api/combinacoes?n=5&estrategia=equilibrada');
  if(!d) return;
  const el = document.getElementById('combos-container');
  let html = '';
  d.combinacoes.forEach(function(combo,i){
    let balls = '';
    combo.forEach(function(n){
      const tipo = hotNums.has(n)?'hot':coldNums.has(n)?'cold':'warm';
      balls += '<div class="ball '+tipo+'">'+String(n).padStart(2,'0')+'</div>';
    });
    html += '<div class="combo-row" onclick="copiarCombo(['+combo.join(',')+'],this)">';
    html += '<span class="combo-idx">'+(i+1)+'</span><div class="balls">'+balls+'</div>';
    html += '<button class="copy-btn">⎘</button></div>';
  });
  el.innerHTML = html;
}

function copiarCombo(combo,row){
  navigator.clipboard.writeText(combo.join(' - '));
  const btn=row.querySelector('.copy-btn');
  btn.textContent='✓';
  setTimeout(function(){btn.textContent='⎘';},1500);
}

/* ── Histórico ── */
async function loadHistorico(){
  const d = await api('/api/historico?limite=8');
  if(!d) return;
  const tbody = document.getElementById('historico-tbody');
  if(!d.sorteios.length){
    tbody.innerHTML='<tr><td colspan="3" style="color:var(--muted)">Sem dados.</td></tr>';
    return;
  }
  let html = '';
  d.sorteios.forEach(function(s){
    let balls = '';
    [s.n1,s.n2,s.n3,s.n4,s.n5,s.n6].filter(Boolean).forEach(function(n){
      balls += '<div class="mini-ball">'+n+'</div>';
    });
    html += '<tr><td style="color:var(--muted)">'+s.concurso+'</td>';
    html += '<td><div class="mini-balls">'+balls+'</div></td>';
    html += '<td style="color:var(--muted)">'+(s.data||'—')+'</td></tr>';
  });
  tbody.innerHTML = html;
}

/* ── Apostas ── */
document.head.insertAdjacentHTML('beforeend', '<style>.num-inp{width:56px;height:40px;background:#0a0c10;border:1px solid rgba(255,255,255,0.15);border-radius:8px;color:#e8eaf0;font-size:15px;font-weight:600;text-align:center;outline:none;font-family:JetBrains Mono,monospace}.num-inp:focus{border-color:rgba(245,200,66,0.5)}.aposta-row{display:flex;align-items:center;gap:10px;padding:12px 14px;background:#181d2a;border:1px solid rgba(255,255,255,0.07);border-radius:10px;margin-bottom:8px;flex-wrap:wrap}.aposta-balls{display:flex;gap:5px;flex:1}.aball{width:30px;height:30px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-family:JetBrains Mono,monospace;font-size:11px;font-weight:600;flex-shrink:0}.aball-normal{background:rgba(155,142,214,0.12);color:#9b8ed6;border:1px solid rgba(155,142,214,0.2)}.aball-hit{background:rgba(0,212,170,0.2);color:#00d4aa;border:1px solid rgba(0,212,170,0.4)}.aball-miss{background:rgba(255,77,109,0.12);color:#ff4d6d;border:1px solid rgba(255,77,109,0.2)}.res-win{background:rgba(0,212,170,0.08);border:1px solid rgba(0,212,170,0.25);border-radius:12px;padding:18px 22px;margin-bottom:10px}.res-lose{background:rgba(255,77,109,0.06);border:1px solid rgba(255,77,109,0.2);border-radius:12px;padding:18px 22px;margin-bottom:10px}</style>');

function toggleApostaForm(){
  const f=document.getElementById('aposta-form');
  f.style.display=f.style.display==='none'?'block':'none';
}

async function registarAposta(){
  const inps=[...document.querySelectorAll('.num-inp')];
  const nums=inps.map(function(i){return parseInt(i.value);}).filter(function(n){return n>=1&&n<=45;});
  if(nums.length!==6){ document.getElementById('aposta-msg').textContent='⚠️ Introduz 6 números válidos (1-45)'; return; }
  if(new Set(nums).size!==6){ document.getElementById('aposta-msg').textContent='⚠️ Os números não podem repetir'; return; }
  const concurso=document.getElementById('concurso-inp').value.trim();
  const r=await api('/api/apostas',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({numeros:nums,concurso:concurso,jogo:'totoloto',custo:100})});
  if(r&&r.status==='ok'){
    document.getElementById('aposta-msg').textContent='✅ Aposta guardada!';
    inps.forEach(function(i){i.value='';});
    document.getElementById('concurso-inp').value='';
    setTimeout(function(){document.getElementById('aposta-msg').textContent='';},2500);
    await loadApostas();
  }
}

async function loadApostas(){
  const d=await api('/api/apostas?limite=10');
  if(!d||!d.apostas.length){
    document.getElementById('apostas-lista').innerHTML='<div style="color:var(--muted);font-size:13px;padding:8px 0">Sem apostas registadas ainda.</div>';
    return;
  }
  let html='';
  d.apostas.forEach(function(a){
    const nums=[a.n1,a.n2,a.n3,a.n4,a.n5,a.n6];
    let balls='';
    nums.forEach(function(n){ balls+='<div class="aball aball-normal">'+String(n).padStart(2,'0')+'</div>'; });
    let acLabel='';
    if(a.verificado){
      acLabel = a.acertos>0 ? '<span style="color:#00d4aa;font-weight:600">'+a.acertos+' acertos ✓'+(a.ganho>0?' · '+fmt(a.ganho)+' ECV':'')+'</span>' : '<span style="color:#ff4d6d">0 acertos</span>';
    } else {
      acLabel='<span style="color:#5a6070">Por verificar</span>';
    }
    html+='<div class="aposta-row"><div style="font-size:10px;color:var(--muted);min-width:60px">'+(a.concurso||'—')+'</div><div class="aposta-balls">'+balls+'</div><div style="font-size:12px">'+acLabel+'</div><div style="font-size:10px;color:var(--muted)">'+a.jogado_em.slice(0,10)+'</div></div>';
  });
  document.getElementById('apostas-lista').innerHTML=html;
}

async function verificarResultados(){
  const msg=document.getElementById('verificar-msg');
  const div=document.getElementById('resultado-verificacao');
  msg.textContent='⏳ A verificar...';
  const d=await api('/api/verificar/ultimo');
  if(!d){msg.textContent='⚠️ Erro ao verificar.';return;}
  if(d.erro){msg.textContent='ℹ️ '+d.erro;return;}
  if(d.info){msg.textContent='ℹ️ '+d.info;return;}
  msg.textContent='';
  const sorteados=d.numeros_sorteados||[];
  let sorteadosStr='';
  sorteados.forEach(function(n){ sorteadosStr+='<span style="font-family:monospace;background:rgba(245,200,66,0.15);padding:2px 7px;border-radius:5px;color:#f5c842;margin:0 2px">'+String(n).padStart(2,'0')+'</span>'; });
  let rows='';
  (d.resultados||[]).forEach(function(r){
    const cls=r.ganhou?'res-win':'res-lose';
    let balls='';
    (r.numeros_jogados||[]).forEach(function(n){
      const hit=(r.numeros_certos||[]).includes(n);
      balls+='<div class="aball '+(hit?'aball-hit':'aball-miss')+'">'+String(n).padStart(2,'0')+'</div>';
    });
    const ganhoLine=r.ganhou?'<div style="font-size:13px;color:#00d4aa;margin-top:6px">💰 Ganho: <b>'+fmt(r.ganho)+' ECV</b></div>':'';
    rows+='<div class="'+cls+'"><div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap"><div class="aposta-balls">'+balls+'</div><div><div style="font-size:14px;font-weight:600;color:'+(r.ganhou?'#00d4aa':'#ff4d6d')+'">'+r.descricao+'</div><div style="font-size:12px;color:var(--muted);margin-top:3px">Acertos: '+((r.numeros_certos||[]).join(', ')||'nenhum')+'</div>'+ganhoLine+'</div></div></div>';
  });
  const totalLine=d.total_ganho>0?'<div style="color:#00d4aa">🏆 Total ganho: <b>'+fmt(d.total_ganho)+' ECV</b></div>':'<div style="color:#ff4d6d">❌ Sem prémios desta vez</div>';
  div.style.display='block';
  div.innerHTML='<div style="padding:16px;background:rgba(245,200,66,0.05);border:1px solid rgba(245,200,66,0.15);border-radius:10px;margin-bottom:14px"><div style="font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:8px">Resultado — Concurso '+d.concurso+'</div><div style="font-size:13px;color:var(--muted);margin-bottom:6px">Números sorteados:</div><div style="margin-bottom:10px">'+sorteadosStr+'</div>'+totalLine+'<div style="font-size:12px;color:var(--muted);margin-top:4px">Gasto: '+fmt(d.total_gasto)+' ECV · Lucro: <span style="color:'+(d.lucro_total>=0?'#00d4aa':'#ff4d6d')+'">'+fmt(d.lucro_total)+' ECV</span></div></div>'+rows;
  await loadApostas();
}

/* ── Atualizar dados ── */
async function triggerScrape(){
  const sp=document.getElementById('scrape-spinner');
  sp.style.display='block';
  try{ await fetch(API+'/api/scrape',{method:'POST'}); await loadAll(); }
  finally{ sp.style.display='none'; }
}

/* ── LottoVision ── */
const lvMsgs = document.getElementById('lv-messages');

function addLVMessage(text, isUser){
  const d = document.createElement('div');
  d.className = 'lv-msg '+(isUser?'lv-msg-user':'lv-msg-bot');
  if(isUser){ d.textContent=text; }
  else{
    // Format combinations and bold
    let formatted = text
      .replace(/(\d{2})\s*[-–]\s*(\d{2})\s*[-–]\s*(\d{2})\s*[-–]\s*(\d{2})\s*[-–]\s*(\d{2})\s*[-–]\s*(\d{2})/g, '<span class="combo-highlight">$1 · $2 · $3 · $4 · $5 · $6</span>')
      .replace(/\*\*(.*?)\*\*/g,'<b>$1</b>')
      .replace(/\*(.*?)\*/g,'<i>$1</i>')
      .replace(/\n/g,'<br>');
    d.innerHTML = formatted;
  }
  lvMsgs.appendChild(d);
  lvMsgs.scrollTop = lvMsgs.scrollHeight;
  return d;
}

function showTyping(){
  const d = document.createElement('div');
  d.className='lv-msg lv-msg-bot'; d.id='lv-typing';
  d.innerHTML='<div class="typing"><span></span><span></span><span></span></div>';
  lvMsgs.appendChild(d);
  lvMsgs.scrollTop=lvMsgs.scrollHeight;
}
function removeTyping(){ const t=document.getElementById('lv-typing'); if(t) t.remove(); }

async function sendToLV(){
  const inp=document.getElementById('lv-input');
  const btn=document.getElementById('lv-send-btn');
  const msg=inp.value.trim();
  if(!msg) return;
  inp.value=''; btn.disabled=true;
  addLVMessage(msg, true);
  showTyping();
  try{
    const r = await fetch(API+'/api/lottovision',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mensagem:msg})});
    const data = await r.json();
    removeTyping();
    addLVMessage(data.resposta||'Sem resposta.');
  }catch(e){
    removeTyping();
    addLVMessage('⚠️ LottoVision temporariamente indisponível. Verifica a tua API key no ficheiro .env');
  }finally{ btn.disabled=false; inp.focus(); }
}

function sendSuggestion(el){
  document.getElementById('lv-input').value = el.textContent.replace(/^[^\w🎲🔥💰📊🃏]*/,'');
  sendToLV();
}

/* ── Load All ── */
async function loadAll(){
  // Carrega sequencialmente — garante que frequencias carrega antes das combinacoes
  try { await loadJackpot(); } catch(e){ console.warn('jackpot',e); }
  try { await loadRecomendacao(); } catch(e){ console.warn('rec',e); }
  try { await loadBudget(); } catch(e){ console.warn('budget',e); }
  try { await loadFrequencias(); } catch(e){ console.warn('freq',e); }
  try { await gerarCombinacoes(); } catch(e){ console.warn('combos',e); }
  try { await loadModoMes(); } catch(e){ console.warn('modo',e); }
  try { await loadProximoMes(); } catch(e){ console.warn('prox',e); }
  try { await loadBencao(); } catch(e){ console.warn('bencao',e); }
  try { await loadEvolucao(); } catch(e){ console.warn('evo',e); }
  try { await loadHistorico(); } catch(e){ console.warn('hist',e); }
  try { await loadApostas(); } catch(e){ console.warn('apostas',e); }
}
loadAll();
</script>
</body>
</html>
